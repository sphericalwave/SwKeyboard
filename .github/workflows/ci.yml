name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
jobs:
  test:
    name: test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Run package tests (macOS)
        run: swift test
      - name: Compile check for iOS (no simulator boot)
        run: >
          xcodebuild -scheme SwKeyboard
          -destination 'generic/platform=iOS Simulator'
          -derivedDataPath .build/dd build

  screenshots:
    name: screenshots
    if: github.event_name == 'pull_request'
    runs-on: macos-15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
      - name: Render screenshots + sync README
        run: GEN_SCREENSHOTS=1 swift test --filter ScreenshotGenTests
      - name: Commit generated assets
        run: |
          if [ -n "$(git status --porcelain Docs/img README.md)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git add Docs/img README.md
            git commit -m "docs: regenerate README screenshots"
            git push
          else
            echo "No screenshot/README changes."
          fi
